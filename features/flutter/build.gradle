plugins {
    id 'com.android.dynamic-feature'
    id 'kotlin-android'
}
android {
    compileSdkVersion versions.compileSdk
    defaultConfig {
        minSdkVersion 21
        targetSdkVersion versions.targetSdk
    }

    buildFeatures {
        viewBinding true
    }

    configurations.all {
        exclude group: 'androidx.annotation', module: 'annotation-jvm'
        exclude group: 'androidx.media', module: 'media'
    }
}

dependencies {
    implementation project(":app")

    implementation "com.example.flutter_module:flutter_debug:1.0"
    implementation "com.trustingsocial.trust_vision_plugin:trust_vision_plugin_debug:1.0"
}